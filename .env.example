# Public canonical origin, without a trailing slash. Set before production builds.
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Optional server-only contact delivery. All three are needed to send messages.
RESEND_API_KEY=
CONTACT_EMAIL=hello@example.com
# Use an address on a domain verified with Resend.
CONTACT_FROM=
